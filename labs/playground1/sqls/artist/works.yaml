urlPath: /artist/:id/works
request:
  - fieldName: id
    fieldIn: path
    description: constituent id
    validators:
      - required
  - fieldName: acq_lt
    fieldIn: query
    description: acquired data latter than
    validators:
      - date