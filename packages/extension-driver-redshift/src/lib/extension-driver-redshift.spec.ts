import { extensionDriverRedshift } from './extension-driver-redshift';

describe('extensionDriverRedshift', () => {
  it('should work', () => {
    expect(extensionDriverRedshift()).toEqual('extension-driver-redshift');
  });
});
