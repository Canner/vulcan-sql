{
  "name": "admin-ui",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/admin-ui",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": [
        "{options.outputPath}"
      ],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/packages/admin-ui"
      },
      "configurations": {
        "development": {
          "outputPath": "packages/admin-ui"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "admin-ui:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "admin-ui:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "admin-ui:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "admin-ui:build:production"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "packages/admin-ui/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    },
    "build-docker": {
      "dependsOn": [
        "build"
      ],
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "cp docker/admin-ui/Dockerfile dist/packages/admin-ui",
          "cp docker/admin-ui/.dockerignore dist/packages/admin-ui",
          "docker build -t admin-ui:latest dist/packages/admin-ui"
        ]
      }
    },
    "generate-gql": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "yarn graphql-codegen --config packages/admin-ui/codegen.yml"
        ]
      }
    },
    "knex-migrate:latest": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "yarn knex migrate:latest --cwd packages/admin-ui"
        ]
      }
    },
    "knex-migrate:rollback": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "yarn knex migrate:rollback --cwd packages/admin-ui"
        ]
      }
    }
  },
  "tags": []
}